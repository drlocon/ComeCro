<h1>ユーザー情報編集画面</h1>
<%= form_with model: @user do |f| %>
  <h4>プロフィール画像</h4>
  <%= f.file_field :profile_image, accept: "image/*" %>
  <h4>名前</h4>
  <%= f.text_field :name %>
  <h4>プロフィール</h4>
  <%= f.text_field :profile %>
   <%= f.submit "編集内容を保存" %>
<% end %>
