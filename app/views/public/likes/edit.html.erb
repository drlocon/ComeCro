<h1>編集画面</h1>
<%= form_with model: @like do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>内容</h4>
  <%= f.text_area :content %>
  
  <h4>画像</h4>
  <%= f.file_field :like_image, accept: "image/*" %>
  
  <h4>タグ</h4>
  <%= f.text_field :tag_name %>
  
  <%= f.submit '更新' %>
<% end %>
