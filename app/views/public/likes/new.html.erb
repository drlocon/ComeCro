<h1>新規投稿</h1>
<%= form_with mode: @like_new do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>内容</h4>
  <%= f.text_area :content %>
  
  <h4>画像</h4>
  <%= f.file_field :like_image, accept: "image/*" %>
  
  <%= f.submit '投稿' %>
<% end %>
